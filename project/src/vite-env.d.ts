/** Injected by vite.config.ts's `define`, from package.json's version field. */
declare const __APP_VERSION__: string;

/** Injected by vite.config.ts's `define`: the git commit, branch, UTC build time and dirty flag captured when the bundle was built. */
declare const __BUILD_INFO__: import("./buildInfo").BuildInfo;
